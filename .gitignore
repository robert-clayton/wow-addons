*.bak
*.orig
.DS_Store
Thumbs.db
*.swp
*~
addons/*/Libs/MidnightUI-1.0
.claude/
dist/

# The content database is a BUILD ARTIFACT, not a source. The curated Lua under
# addons/ is the source of truth; scripts/db/run.sh rebuilds the database from
# it to validate. Committing a binary nothing can diff, review or merge would
# make it look authoritative when it is derived.
build/
data/collectionist.db

__pycache__/
